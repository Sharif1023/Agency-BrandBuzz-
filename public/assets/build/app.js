(() => { const n = document.querySelector("[data-menu-toggle]"), r = document.querySelector("[data-mobile-menu]"); if (n && r) { const e = () => { r.hidden = !0, n.setAttribute("aria-expanded", "false") }; n.addEventListener("click", () => { const t = n.getAttribute("aria-expanded") === "true"; n.setAttribute("aria-expanded", String(!t)), r.hidden = t }), document.addEventListener("keydown", t => { t.key === "Escape" && (e(), n.focus()) }), r.querySelectorAll("a").forEach(t => t.addEventListener("click", e)) } const o = document.querySelector("[data-admin-toggle]"), i = document.querySelector("[data-admin-sidebar]"); o && i && (o.addEventListener("click", () => { const e = i.classList.toggle("is-open"); o.setAttribute("aria-expanded", String(e)) }), document.addEventListener("keydown", e => { e.key === "Escape" && (i.classList.remove("is-open"), o.setAttribute("aria-expanded", "false")) })), document.querySelectorAll("[data-confirm]").forEach(e => e.addEventListener("submit", () => { })), document.querySelectorAll("[data-image-input]").forEach(e => { let t; e.addEventListener("change", () => { var l; const a = document.getElementById(e.dataset.imageInput), c = (l = e.files) == null ? void 0 : l[0]; !a || !c || !/^image\/(jpeg|png|webp|gif)$/.test(c.type) || (t && URL.revokeObjectURL(t), t = URL.createObjectURL(c), a.src = t, a.hidden = !1) }) }); const s = document.querySelector("[data-slug-source]"), d = document.querySelector("[data-slug-target]"); if (s && d) { let e = !d.value; d.addEventListener("input", () => { e = !d.value }), s.addEventListener("input", () => { e && (d.value = s.value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "")) }) } document.querySelectorAll("[data-testimonial-controls]").forEach(e => { const t = document.getElementById(e.dataset.testimonialControls); e.querySelectorAll("button").forEach(a => a.addEventListener("click", () => t == null ? void 0 : t.scrollBy({ left: Number(a.dataset.direction) * 340, behavior: window.matchMedia("(prefers-reduced-motion: reduce)").matches ? "instant" : "smooth" }))) }), document.querySelectorAll("[data-password-toggle]").forEach(e => e.addEventListener("click", () => { const t = document.getElementById(e.dataset.passwordToggle); if (!t) return; const a = t.type === "password"; t.type = a ? "text" : "password", e.textContent = a ? "Hide" : "Show", e.setAttribute("aria-pressed", String(a)) })) })();
